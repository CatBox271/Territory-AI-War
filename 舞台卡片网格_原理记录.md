# 舞台卡片网格 / 底板 —— 原理与排查记录

> 涉及：`Assets/C#/UI显示/ContainerMesh.cs`、`Assets/Shader/ContainerMesh.shader`、
> `Assets/Prefab/Scene/Container.prefab`（+ 变体 `Mesh.prefab`）、`Assets/Scenes/SampleScene.unity`、
> `Assets/pic/白图.png`（新增）

## 0. 先看这条（改场景文件的人）

场景 `.unity` 是在 Unity 之外被改过的。如果 Unity 里正开着 `SampleScene`，**先让它重新加载**
（File → Reload，或重新打开场景）再继续编辑，否则编辑器内存里那份一保存就会把改动覆盖回去。

## 1. 症状

- 「升级选择」演出里，卡片底板（`BackgroundOutline` 那个四边形）全都显示成**同一个尺寸**
  （实测正好 **1.35 × 1.35**），硬边方块、圆角没了；
- 在 Inspector 上手动点 `ContainerMesh` 的 refresh / 重建 能恢复正常，**代码里调 `Rebuild()` 不行**；
- 手动点其中一张卡重建，**别的卡片的底板会跟着一起变**。

## 2. 画法：为什么"可见底板 = min(形状, quad)"

`ContainerMesh` 的底板是「一块 quad + 片元里按 SDF 画圆角矩形」，两边分工：

- **quad 顶点**由 C# 烘焙：`FillQuad` 里 `hx = size.x * 0.5 + edge_padding`
  → 四边形 = `外框尺寸 + 2 × edge_padding`（`Container.prefab` 上 `edge_padding = 0.25`）；
- **形状/颜色**由 shader 从 `MaterialPropertyBlock` 拿（`_ShapeSize / _ShapeCenter / _Radius / _WallWidth / _WallColor / _BackColor`）。

关键约束在 shader 里：

- `vert` 把顶点原样传出：`o.local = v.vertex.xy;`（`ContainerMesh.shader:74`）
- `frag` 在这个空间里算 SDF，再用覆盖率收边：`covOuter = saturate(0.5 - dOuter / aa)`（`:105`）
- 最终 `c.a = lerp(_BackColor.a, _WallColor.a, t) * covOuter;`（`:117`）

**片元只能在 quad 覆盖到的像素上画 —— shader 只能把图形画小，永远画不出四边形之外。**
所以：

- 改材质 / `_ShapeSize` / `_Radius`，底板**不会变大**（被 mesh 卡死）；
- 可见尺寸"精确等于某个值" ⇒ 那一定是 **quad 的尺寸**，不是形状参数的问题。

### 1.35 是怎么算出来的

`1.35 = 0.85 + 2 × 0.25`

- `0.85` = 「升级选择」里**图标卡**的 `item.size`（`UpgradeChoiceScene` 里 `new Vector2(0.85f, 0.85f)`）；
- `0.25` = `Container.prefab` 的 `edge_padding`（两边各加一圈）。

全工程没有任何地方写死 `1.35`。图标卡恰好是每张选项卡里**最后**建的一块
（`BuildOptions` 的顺序是 board → title → label → icon）。

## 3. 根因：过程网格不是「每实例独占」

`ContainerMesh.EnsureMesh` 原来是：

```csharp
Mesh mesh = mf.sharedMesh;          // ← 接管别人留在这个 MeshFilter 上的网格
if (mesh == null) { mesh = new Mesh { hideFlags = HideFlags.DontSave }; mf.sharedMesh = mesh; }
FillQuad(mesh, size, center);       // ← 无条件往里写
```

`Mesh` 是 `HideFlags.DontSave` 的**过程网格**，不会存盘，但会活在内存里：

1. 编辑器里点一次 `refresh` / 重建（`OnValidate` → `Rebuild`）会在 **prefab 资产上**建出一份网格并挂到它的
   `MeshFilter.m_Mesh` 上（`m_Mesh` 是序列化字段）；
2. 之后运行时每次 `Instantiate`，克隆体的 `m_Mesh` 都先指向**同一份**；
3. `FillQuad` 无条件写进去 ⇒ **全场共用一份 quad**，谁最后重建，谁的尺寸留在台上。

于是三个症状全对上了：

- 代码建卡的顺序固定，最后写的是图标卡 → 所有底板都是 1.35×1.35；
- 手动点某一张，写进去的是你点的那张的尺寸 → 那张"看着正常了"，**同时别的卡被改成这个尺寸**；
- 这跟刷新频率、跟 `Time.timeScale` 都无关。

> 顺带：`Time.timeScale = 0` 不降低刷新频率。`Update` / `LateUpdate` / `yield return null` /
> `WaitUntil` / `OnValidate` / 渲染照旧每帧跑；被掐掉的只有 `FixedUpdate`+物理、`deltaTime`/`Time.time`、
> `WaitForSeconds`/`Invoke`、Animator(Normal)、ParticleSystem(Normal)，以及 **shader 里的 `_Time`**。
> 本工程舞台这三个 shader（`ContainerMesh` / `SpriteMeshDot` / `SimpleGrabPassBlur`）**都不读 `_Time`**，
> 用 `_Time` 的只有 `SpriteBurn`、`SpriteRainbowFlow`、`SpriteRingEnchant`。

## 4. 修复

1. **`ContainerMesh.EnsureMesh`：网格归属跟着组件实例走**
   - 加 `ownedMesh`（`[NonSerialized]`），没有就 `new Mesh`；
   - `if (mf.sharedMesh != ownedMesh) mf.sharedMesh = ownedMesh;` —— 实例化时残留的资产共享网格会被换掉；
   - `OnDestroy` 里销毁，不再靠"永远不回收"的 `DontSave` 兜着。
2. **`ContainerMesh.shader`：墙厚为 0 时整块取背景色**
   - `wall_width = 0`（舞台 `StageStyle.Wall = 0`）时 `innerHalf` 与内角半径都等于外框，
     `dInner ≡ dOuter` ⇒ `t = 1 - covOuter`，整块被画成 `_WallColor`、`_BackColor` 只剩边缘一圈 AA。
   - 现在按 `wallSum = _WallWidth` 之和判断：没有墙 ⇒ `t = 0` ⇒ 整块 `_BackColor`。
   - ⚠️ 这条会改观感：舞台卡片底板由"墙色"变成脚本真正传的底色（`StageStyle.PanelBg`）。
3. **`ContainerMesh.ResolveMaterial`：材质副本缓存 + 释放**
   - 原来第一次返回 `new Material(...)` 却没写回字段：第二次 `Rebuild` 又会换回**原件**（材质身份前后不一致），
     而没人销毁的副本一直留在场景里（场景里那个 `叙事毛玻璃 (Instance) (Instance) … ×18` 就是这类泄漏）。
   - 现在副本存进 `instanceMaterial`，`OnDestroy` → `ReleaseInstanceMaterial()` 释放；
     静态兜底材质 `_fallbackMaterial` 共享、不销毁。
4. **场景里 49 个丢失的占位 sprite → `Assets/pic/白图.png`**
   - `SampleScene` 里 49 个 `SpriteRenderer`（墙体 `MonoWall(Clone)`、掩码/生成区域 `0`~`5`、`Square (1~3)`、
     `Background` 毛玻璃底板…）引用的是同**一个** sprite：guid `311925a002f4447b3a28927169b83ea6`
     —— 它在本工程里根本不存在（场景是从别处拷过来的）。
   - 新增 `白图.png`：100×100 纯白，PPU 100 ⇒ 世界尺寸正好 **1×1 单位**，
     和这些对象里 `m_Size / newSize {1,1}` 完全一致；它们本来就靠 `m_Color` 染色，所以一个白方块就够。
   - Unity 里如果发现没生效：确认 `白图.png` 的 Texture Type 是 **Sprite (2D and UI)**、PPU 100。

## 5. 排查手法（可复用）

- **尺寸不对，先分清"形状参数错"还是"网格错"**：抓两张卡的
  `BackgroundOutline.GetComponent<MeshFilter>().sharedMesh.GetInstanceID()` —— 相同就是共用网格。
- **把一个写死的数字做算术解码**：`1.35 = 0.85 + 2×0.25` 直接把范围缩到"图标卡 + edge_padding"。
- **查场景里丢的资源**：把场景里所有 `guid: <32hex>` 抓出来，和工程（含 `Library/PackageCache`）
  所有 `.meta` 的 guid 取差集，差出来的就是丢失引用。
  （注意 `file:` 依赖的本地包在工程外，例如 TMP 在 `C:\Users\m1872\Desktop\Unity\包\...`，要单独算进去。）
- **别用"手点一下就好了"当结论**：手点成功往往只是把共享状态改成了你想要的值，换台机器/换个顺序就复现。

## 6. 遗留 / 同款坑

- `SceneSpriteDisplay.ApplyToChild` 里也是 `Mesh mesh = mf.sharedMesh;` 的写法。
  它的 `SM_` 子物体是运行时新建的，暂时碰不到这次的问题，但**要复制同样的归属判断**才安全。
- 场景里还剩一个丢失引用：prefab 实例「立绘1」的 `Animator.m_Controller`
  （guid `eca2e0888ce99664f9002ba07c68112c`，来自原工程，本工程没有这个 controller）。
- 凡是"编辑器里点一下能好、代码里不行"的过程网格 / 过程材质，都先怀疑**归属（ownership）**：
  谁创建的、谁复用、谁销毁。
